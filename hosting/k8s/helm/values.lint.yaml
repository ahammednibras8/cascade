runtimeSecret:
  existingSecret: cascade-runtime

apiSecret:
  existingSecret: cascade-api

dashboardSecret:
  existingSecret: cascade-dashboard

api:
  image:
    tag: lint

dashboard:
  oidc:
    issuerUrl: https://identity.example.test
    clientId: cascade-dashboard
    redirectUri: https://dashboard.example.test/auth/callback
  image:
    tag: lint

deploymentRunner:
  image:
    tag: lint

workers:
  image:
    tag: lint
