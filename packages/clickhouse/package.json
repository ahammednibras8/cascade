{
  "name": "@cascade/clickhouse",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate": "goose -dir migrations clickhouse \"$CLICKHOUSE_NATIVE_URL\" up",
    "rollback": "goose -dir migrations clickhouse \"$CLICKHOUSE_NATIVE_URL\" down",
    "status": "goose -dir migrations clickhouse \"$CLICKHOUSE_NATIVE_URL\" status",
    "create": "goose -dir migrations create"
  }
}
